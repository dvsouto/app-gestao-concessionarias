import { StyleSheet, Platform } from 'react-native';

const styles = StyleSheet.create({
  viewVeicleData: {
    width: '100%',
    alignSelf: 'stretch',
    alignItems: 'center'
  }
});

export default styles;
